# Build example codes that use simplemc::simplemc-serialize-variant.
#
# The following targets are defined:
# ---------------------------------
# doc_variant_serializer: Doc example showing the concrete backend as a runtime choice.

simplemc_add_examples(
    doc_variant_serializer
)
